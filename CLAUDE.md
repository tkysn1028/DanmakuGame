# 弾幕STG (Java / Swing)

## このプロジェクトの目的

**プログラミング技術に触れ、ロジックと設計を突き詰めること**が目的。遊べるゲームを完成させることや、
ユーザー体験を磨くことは主目的ではない。音・画像・演出の作り込みは優先度が低く、図形描画のままでよい。

現在の目標は「1ステージを作り切る」。スコープは意図的に小さく保つ。

## 作業スタイル（重要）

- **設計判断は開発者本人が行う。** AIの役割は、選択肢とトレードオフを提示すること
- コードを丸ごと生成して渡すより、**なぜそう書くかの説明**のほうが価値が高い
- 詰まったときは、答えより**切り分け方**を求めている
- 長い回答より、短く的確な回答を好む。設計相談と、詰まりの解消は別物として扱う
- 実測を優先する。AIの指摘が外れていることもあるので、症状の確認方法を示すこと

## 開発環境

- Windows 11 / JDK 25 (Eclipse Temurin) / VS Code + Extension Pack for Java
- ビルドツールなし。`javac *.java` で通る構成を維持している
- Redmine でタスク管理（スコープ固定のため）

## 将来の方向性
- 将来的にはJava SwingからLibGDXに移植予定。
- 現在のGamePanelクラスにJava Swingのコードを集約するようにし、他クラスでjava.awtパッケージのクラスを参照していた場合は都度指摘する。

## 設計方針

### レイヤ分離

依存の向きを守る。**矢印が逆になったら設計が壊れている、と判断する。**

```
ui     → core, entity, util      （描画は全部を知る）
core   → entity, patterns, util
entity → util
util   → 何にも依存しない
```

- **`entity` / `core` パッケージから `java.awt` を参照しない。** LibGDX 移植時に触る範囲を
  `GamePanel` だけに閉じ込めるため。色は描画側（`ColorConst`）が決める
- `Input` クラスは AWT のキーコードを遮断するための緩衝材。
  `KeyEvent.VK_*` の翻訳は `GamePanel` の責務
- `Game` はオーケストレーター。for ループを持たず、各コンポーネントへの委譲だけにする

### 状態の持ち方

- **可変状態を static にしない。** `Game` はインスタンスとして複数存在しうる
  （ステージ遷移、リトライ、タイトル裏のデモ、テスト）
- static にしてよいのは**定数**（`ConfigConst`）と**純粋関数**（`MathUtil`, `Collision`）だけ
- 乱数はシード固定で `Game` のフィールドに持つ。static にすると決定論性が失われる

### クラスを増やす判断

- **増やさないと困ってから増やす。** 対称性のために作るクラスはたいてい早すぎる
- 弾は `BulletPool` / `ShotPool` で管理（数千発）、敵は `List<Enemy>`（数十体）。
  **扱う量が違うので、非対称でよい**
- 自機弾と敵弾はプールを分ける。種別フラグでの分岐が不要になり、判定ループも小さくなる
- ボスは `Enemy` に統合し、`List` に入れつつ `boss` フィールドで参照も持つ
  （`isBoss` フラグは分岐を増殖させるので使わない）

### 命名

- `Manager` は情報量がゼロなので避ける（`SceneManager` は例外として許容）
- 同じ意味の操作は同じ動詞に揃える（`update` / `step` を混在させない）
- パラメータ名は「変えると何が変わるか」を表す（`step` より `TWIST`、`n` より `SPOKES`）
- クラス名を別のクラス名に命名変更する指示を受けた際は、前提にしていたローカル変数やメンバ変数やメソッド名もすべて変更するようにしてください。

---

## 踏んだ罠（再発しやすい）

### Coroutine

- **ループに `pause` を書き忘れる** → そのループが1フレーム内で走り切る。
  終了条件があれば「動きが一瞬で完了」、`while(true)` なら**ゲーム全体がフリーズ**する。
  操作を受け付けなくなったら、まずこれを疑う
- **コルーチンの中から `scheduler.add()` を呼ぶと `ConcurrentModificationException`**。
  `Scheduler` は追加を `pending` に溜め、`tick()` の冒頭で合流させる形にしてある。
  別スレッドが原因ではなく、イテレート中にリスト構造を変えたことが原因
- 同じ問題は `enemies` リストでも起きる。回しながら追加する構造に注意

### 当たり判定

- **方向によって作法が違う**
  - 自機弾 × 敵 … `break` しない（連射がちゃんとダメージになるように）
  - 敵弾 × 自機 … 1発当たったら `break`（1フレームに何十発重なっても被弾は1回）
- `forEach` は途中で抜けられない。早期脱出が要るなら拡張for文に戻す
- `dead` / `expired` フラグは、`removeIf` の条件に入れないと弾が残り続け、
  毎フレーム判定に引っかかってHPが削られ続ける

### Swing

- 座標系は **左上原点・Y軸下向き**。角度は時計回りに増え、真上は `-Math.PI / 2`
- `repaint()` は「描画の予約」であって即時描画ではない。
  実際の `paintComponent` の回数はフレーム数と一致しない（カクツキの一因）
- `javax.swing.Timer` は精度が低い。`1000/60` は整数除算で 16 になる。
  改善するなら専用スレッドで `System.nanoTime()` の目標時刻を累積する方式へ
- `super.paintComponent(g)` を忘れると前フレームが消えず軌跡が残る
- `setFocusable(true)` と `requestFocusInWindow()` の両方がないとキー入力が効かない
- `main` の9行は順序依存（`pack()` → `setLocationRelativeTo()` → `setVisible()` → `requestFocusInWindow()`）
- 「押した瞬間」の検出には前フレームの状態が要る。
  押しっぱなしだとシーン遷移が一瞬で通過する

### その他

- `Math.atan2(y, x)` は**引数がYが先**。逆にすると90度ずれる
- 斜め移動は `0.7071`（1/√2）を掛けないと1.41倍速くなる
- 位置を `sin(frame / speed)` で求める方式は、`speed` を変えた瞬間に位相が飛ぶ。
  **位相を累積する形（`phase += angularSpeed`）にすること**
- 画面外の弾を消さないと、数分で数万発が蓄積して重くなる（症状が遅れて出るので厄介）

---
